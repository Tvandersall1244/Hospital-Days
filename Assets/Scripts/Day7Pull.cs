using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Day7Pull : MonoBehaviour
{
    public int day = 7;
    // Start is called before the first frame update
    void Start()
    {
        if(day == 7){
            
        }
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
